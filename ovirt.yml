---

plugin: ovirt.ovirt.ovirt
ovirt_url: "{{ engine_fqdn }}"
ovirt_cafile: "{{ engine_cafile | default(omit) }}"
ovirt_username: "{{ engine_user }}"
ovirt_password: "{{ engine_password }}"
ovirt_insecure: false
